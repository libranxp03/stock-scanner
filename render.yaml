services:
  - type: web
    name: stock-intel-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run dashboard_app.py
    autoDeploy: true

cronJobs:
  - name: tier1-scan
    schedule: "*/45 8-21 * * *"
    command: python tier1_scanner.py
